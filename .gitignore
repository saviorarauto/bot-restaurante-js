# Dependências do Node
node_modules/

# Arquivos de ambiente (NUNCA subir senhas e chaves)
.env

# Sessão e Cache do WhatsApp (MUITO IMPORTANTE IGNORAR)
.wwebjs_auth/
.wwebjs_cache/

# Banco de dados local (Para não sobrescrever ou vazar dados de clientes)
contatos.json

# Pastas de build/compilação do Electron
dist/
build/
out/

# Logs de erro do npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos do Sistema Operacional (Windows/Mac)
.DS_Store
Thumbs.db